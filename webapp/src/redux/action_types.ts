import {id as pluginID} from '../manifest';

export default {
    SET_ACTIVE_POST: `${pluginID}_set_active_post`,
    SHOW_EDITOR_MODAL: `${pluginID}_show_editor_modal`,
    HIDE_EDITOR_MODAL: `${pluginID}_hide_editor_modal`,
    SET_MONACO_THEME: `${pluginID}_set_monaco_theme`,
    SET_USER_PREFERENCES: `${pluginID}_set_user_preferences`,
}
